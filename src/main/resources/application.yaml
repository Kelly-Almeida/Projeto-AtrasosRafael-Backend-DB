spring:
  application:
    name: AtrasosRafael

  datasource:
    url: jdbc:mysql://localhost:3306/AtrasosRafael?createDatabaseIfNotExist=true&serverTimezone=UTC
    username: root
    password: 25164036Ka!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  spring:
    flyway:
      locations:
        - classpath:db/migration/trigger
      baseline-on-migrate: true
      baseline-version: '0'